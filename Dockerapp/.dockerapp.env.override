## Docker override - only these variables override Dockerfile defaults
## All other variables (FLASK_APP, CUSTOM_PHOTO, CUSTOM_HEADER, FONT_COLOR) use Dockerfile values

BG_COLOR=lightgreen
FLASK_ENV=docker override (.dockerapp.env.override)